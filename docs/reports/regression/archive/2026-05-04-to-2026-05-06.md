# 2026-05-04 至 2026-05-06 回归归档

> 本分片承接从 `docs/reports/regression/current.md` 滚动迁移的历史窗口记录，覆盖第三十四阶段冲刺收口与第三十五阶段 Postgres 子切片关闭。

## 2026-05-06 第三十五阶段 Postgres P0：首页 popular posts 显式 `limit` 查库收敛关闭

### 范围

- 目标：关闭第三十五阶段 Postgres P0 的首页 popular posts 子切片，并补齐数据库级 live sample 证据。
- 覆盖面：`/api/posts` 显式 `limit=3` 热点公开读路径、同口径缺省 `limit` 对照路径、对应 API 定向测试与规划记录。
- 非目标：不扩写到其它公开读链路，不把 runtime cache 命中统计替代数据库级证据。

### 结论

- 显式 `limit=3` 请求不再触发前置 settings 查库，仅保留 `momei_post` 查询指纹。
- 缺省 `limit` 对照请求仍保留 `momei_setting` 读取，形成可验证 delta。
- 定向测试与 typecheck 通过，满足该子切片关闭条件。

### 验证摘要

- 本地 PostgreSQL 17 + `pg_stat_statements` 对照采样通过。
- `pnpm exec vitest run tests/server/api/posts/index.get.test.ts` 通过。
- `pnpm exec nuxt typecheck` 通过。

## 2026-05-06 第三十四阶段 coverage 80%+ 冲刺达成

### 范围

- 目标：在 `79.57%` lines 基线上继续冲刺，正式越过 `80%`。
- 覆盖面：后台 settings/editor 的高 ROI 组件切片与全量 coverage checkpoint。
- 非目标：不扩写到更重的后台异步大面。

### 结论

- 多个 settings 组件定向 coverage 抬升到高线（约 `93% - 96%` lines）。
- 全量 `pnpm test:coverage` 最终达到 lines `80.05%`，第三十四阶段 coverage 冲刺条目可关闭。

### 验证摘要

- 受影响组件定向 Vitest 与定向 coverage 全部通过。
- `pnpm test:coverage` 全量 checkpoint 通过，All files 过 `80%` lines。

## 2026-05-05 第三十四阶段 coverage 80%+ 冲刺续推：ai-settings 切片

### 范围

- 目标：在不重跑全量 coverage 的前提下，拿下一块 settings 高 ROI 条件渲染切片。
- 覆盖面：`ai-settings` 组件与同级测试桩收敛。

### 结论

- 测试由单一 happy path 扩展到 `3` 条风险路径。
- 组件局部 lines 从 `44.28%` 提升到 `81.42%`，形成稳定可复用的增量收益。

### 验证摘要

- `pnpm exec vitest run --config` 定向与定向 coverage 通过。
- 定向 ESLint 与文件级诊断通过。

## 2026-05-05 第三十四阶段 `周期性回归执行 (P1)` 关闭

### 范围

- 目标：完成真实 `pnpm regression:phase-close` 阶段关闭回归，并解除依赖安全、导航 E2E、性能预算、回归窗口治理阻塞。

### 结论

- 依赖安全阻塞解除，发布前校验恢复通过。
- 导航 E2E 稳态恢复，`release:check:full` 回到全通过。
- 性能预算误判修复，`test:perf:budget:strict` 通过。
- 回归窗口完成滚动归档并压回健康线。
- `pnpm regression:phase-close` 全链路通过。

### 验证摘要

- `pnpm test:perf:budget:strict` 通过。
- `pnpm regression:phase-close` 通过并产出 Review Gate 证据。

## 2026-05-05 第三十四阶段 `i18n 运行时继续扩面 (P1)` 关闭

### 范围

- 目标：完成 `archives` 公开页装配链路固定 runtime 回归纳管并关闭条目。

### 结论

- `archives` 页面运行时验证已并入固定 `i18n:verify:runtime` 测试矩阵。
- `i18n:audit:missing` 保持 `0`，满足关闭条件。

### 验证摘要

- `pnpm exec vitest run --config vitest.i18n.config.ts pages/archives/index.test.ts` 通过。
- `pnpm i18n:verify:runtime` 通过。

## 2026-05-04 第三十四阶段 `ESLint 下一轮切片 (P1)` 关闭

### 范围

- 目标：按回退口径收口高 ROI production composable 的 `no-explicit-any` 单文件/双文件切片。

### 结论

- 本轮 production composable 显式 `any` 收敛完成，未引入超范围规则扩写。
- 定向 ESLint、定向 Vitest、typecheck 与 lint 通过，满足本条关闭条件。

### 验证摘要

- 定向 ESLint 通过。
- 同级 Vitest 与新增 focused test 通过。
- `pnpm exec nuxt typecheck` 与 `pnpm lint` 通过。

## 2026-05-04 `use-tts-task.ts` `no-explicit-any` 回退切片

### 范围

- 目标：在 `composables` 子桶无新增高 ROI `no-non-null-assertion` 命中前提下，完成 `use-tts-task.ts` 单文件 `no-explicit-any` 收敛。

### 结论

- 将轮询响应与错误分支从显式 `any` 收敛到局部类型与 helper。
- 该单文件切片验证通过，具备可回滚边界。

### 验证摘要

- `pnpm exec vitest run composables/use-tts-task.test.ts` 通过。
- `pnpm exec eslint composables/use-tts-task.ts eslint.config.js --max-warnings 0` 通过。

## 2026-05-04 第三十四阶段 coverage 80%+ 冲刺收口线达成

### 范围

- 目标：先达成第三十四阶段 `>= 78%` 收口线，再继续冲刺 `80%+`。

### 结论

- 多个 settings / notifications / admin 组件与 composable 高 ROI 切片完成。
- 全量 coverage 达到 lines `78.06%`，阶段收口线达成。
- 覆盖率冲刺方法论已回写测试规范。

### 验证摘要

- 定向 Vitest 与定向 ESLint 通过。
- `pnpm test:coverage` 全量 checkpoint 通过。

## 2026-05-04 第三十四阶段 coverage 80%+ 冲刺首轮推进

### 范围

- 目标：先量化缺口并完成首轮高 ROI 定向切片，暂不重跑全量 coverage。

### 结论

- `use-auth-session` hydration/lifecycle 分支与 TTS 直连成功路径守线已落地。
- 定向 coverage 显示首轮切片收益明确，为后续冲刺提供了稳定基线。

### 验证摘要

- `use-auth-session` 与 `post-tts-dialog` 定向 Vitest、定向 coverage 均通过。

## 迁移说明

- 本分片用于减轻 `current.md` 活动窗口体积，保证近线记录可读性与阶段对比效率。
- 如需查看该时段原始详细执行上下文，请优先结合同日期 Review Gate artifact 与对应测试运行产物。
