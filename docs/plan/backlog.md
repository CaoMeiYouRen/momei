# 墨梅 (Momei) 长期规划与积压项 (Backlog)

本文档用于记录暂不进入当前迭代、但具备中长期价值的功能与治理事项。当前阶段执行面请参阅 [项目计划](./roadmap.md)、[待办事项](./todo.md) 与 [待办归档](./todo-archive.md)。

> **维护规则**:
> 1. 新功能需求、非阻塞优化与长期治理事项优先写入本文件，而不是直接写入 `todo.md`。
> 2. 一旦某项 backlog 被正式上收为当前/下一阶段任务，必须从本文件移除，避免重复统计。
> 3. 当前仓库的 backlog 以中文为唯一事实源；翻译文档只保留摘要或跳转说明。

> **最近调整**: 以下事项已于 2026-03-19 上收至第十五阶段，不再在 backlog 中重复保留：
> - AI Agent / Skills 治理、Rules 边界与复用收敛
> - 自动化验证分级、周期性回归与 Review Gate
> - 文档国际化目录重构
> - 日语界面与文档支持

## 当前 backlog

### 待第十五阶段归档后再评估的治理需求（2026-03-20 新增）

以下事项已明确记录为新增需求，但由于第十五阶段尚未完成归档整理，当前**不启动第十六阶段规划**，统一先保留在 backlog 中，待第十五阶段正式归档后再逐项做评分、取舍与阶段上收。

补充说明：

- “回归记录单独文档”已由当前第十五阶段待办承接，并已落到 [regression-log.md](./regression-log.md)，因此不在本节重复保留为 backlog。
- “规划/文档改动提交前也必须 review”已先写入规范，以下 backlog 主要记录后续仍需继续落地的治理、技能、文档与功能事项。

1. **规范文档去重与事实源收敛**
- **重复内容清理**:
    - 整理 `development`、`testing`、`documentation`、`ai-collaboration`、`AGENTS.md` 等文档中的重复检查项、重复门禁与重复流程说明。
    - 优先改为“唯一事实源 + 链接引用”，避免“提交前检查”“Review Gate”“验证矩阵”等内容在多个规范中长期双写。
- **收敛边界**:
    - 明确哪些规则应该保留在项目级权威文档，哪些仅在专项规范中保留最小补充。
    - 防止后续新增规范再次复制已有内容而形成第三份事实源。

2. **规范代码示例最小化**
- **示例裁剪**:
    - 清理开发规范、文档规范及相关指南中没有必要的样板代码示例。
    - 仅保留项目特有、易错、无法靠文字精确表达的最小示例。
- **维护约束**:
    - 为后续规范新增示例建立准入标准，避免文档重新膨胀为教程式大段模板。

3. **AGENTS Git 规范回链收敛**
- **Git 规则去重**:
    - `AGENTS.md` 中关于 Git、Worktree 与提交流程的内容进一步收敛为指向 [Git 规范](../standards/git.md) 的回链。
    - 项目级 AI 规则仅保留最小行为边界，不再重复描述 Git 细则。
- **入口同步**:
    - 同步检查 `CLAUDE.md`、Copilot 入口、agents / skills 中是否仍存在与 Git 规范重复的描述。

4. **Code Auditor 审查闭环强化**
- **审计输出升级**:
    - 强化 `code-auditor` 与相关审查技能，要求结论必须明确失败原因、通过条件、检查点列表与阻塞级别。
    - 避免只给出松散问题列表，导致修改方向不明确。
- **复查一致性**:
    - 明确下一轮审查应基于上一轮问题清单逐条复核，而不是重新从零开始发散式审查。

5. **修复清单对账与临时审查记录机制**
- **问题清单闭环**:
    - 开发者在收到问题列表后必须逐条核对、修复并带回更新后的问题清单进入下一轮审查。
    - 降低相同问题被反复提出但长期未关闭的风险。
- **临时记录机制**:
    - 建立不纳入 Git 的临时审查记录，用于审查中断后的续查、问题对账与状态延续。

6. **提交前固定多轮 Review 流程**
- **流程固化**:
    - 将“任何改动提交前至少 review 一轮，必要时多轮 review”进一步沉淀为固定执行流程。
    - 覆盖代码、配置、脚本、规划、归档、规范与其他 Markdown 文档改动。
- **证据要求**:
    - 明确提交前需要保留的 review 结论、剩余问题与放行条件，避免 review 只有口头表述。

7. **按改动类型执行验证命令的规范收敛**
- **验证矩阵落地**:
    - 继续收敛“代码改动走 `lint` / `typecheck`，Vue / SCSS 等样式改动补 `lint:css`，Markdown / 文档改动补 `lint:md`，测试按风险按需升级”的统一口径。
    - 将该口径同步到开发规范、文档规范、Git 规范、AI 协作规范和相关 skills/agents。
- **边界说明**:
    - 明确 test 是默认补充项而非所有场景一刀切全量执行。

8. **内部技能与外部技能分层治理**
- **技能边界**:
    - 区分项目内部维护、与业务高度相关的内部技能，和从外部同步、本地不维护的通用技术技能。
    - 明确目录边界、更新方式、同步来源与失效处理策略。
- **治理目标**:
    - 避免项目自有知识与外部通用技能混杂，导致维护责任不清。

9. **Skills / Agents 管理规范补齐**
- **库存与生命周期**:
    - 为 skills / agents 的命名、库存、引用、镜像、生命周期、审查、弃用与清理建立正式管理规范。
    - 形成可审计的治理文档，避免后续继续靠经验维护。
- **与 AI 管理细则衔接**:
    - 评估将其并入 AI 管理细则，或单独维护为专项文档。

10. **Full Stack Master 的 PDTFC+ 流程修正**
- **流程纠偏**:
    - 修复 `full-stack-master` 中 PDTFC+ 工作流的错误或歧义。
    - 对齐 Plan / Do / Audit / Validate / Test / Finish 的职责边界与交接顺序。
- **镜像同步**:
    - 同步检查 `.github/` 与 `.claude/` 镜像是否存在相同错误。

11. **封面五维提示词生成模型**
- **五维模型**:
    - 将封面提示词收敛为“类型、配色、渲染、文本、氛围”五个维度。
    - 明确每个维度的输入来源、默认回退与可编辑覆盖策略。
- **自定义性增强**:
    - 提升封面提示词的可配置性与结果一致性，避免生成结果过于随机。

12. **AI 图片生成场景扩展**
- **从封面扩展到配图**:
    - 将图片生成能力从封面扩展到文章配图、专题头图、活动图等更多需要图片的场景。
    - 明确哪些场景适合自动回填，哪些场景仅生成候选图供人工确认。
- **回归与治理**:
    - 补齐扩面后的任务记录、参数约束与编辑流程回归验证。

13. **API 规范与邮件服务设计重写**
- **API 规范重写**:
    - 复核并修正过时的 `docs/standards/api.md` 内容，使其与当前请求层、响应约定和邮件服务边界一致。
- **设计文档对齐**:
    - 对照当前实现重写 [API 设计](../design/api.md)，尤其是邮件服务相关内容。
- **细则下沉**:
    - 不再把邮件模板、邮件变量与投递实现细节长期堆在总规范中，优先拆分到具体模块文档，例如邮件模块设计文档。

14. **安全规范过时内容纠偏**
- **安全口径复核**:
    - 复核 `docs/standards/security.md` 与当前项目安全边界、自动化脚本、依赖治理和权限校验现状的差异。
- **约束收敛**:
    - 移除过时或与其他规范重复的安全细则，仅保留仍然有效的项目级安全红线与回链。

15. **性能规范过时内容纠偏**
- **性能基线复核**:
    - 复核 `docs/standards/performance.md` 与当前 Lighthouse、Bundle、性能预算及治理流程的偏差。
- **边界收敛**:
    - 明确哪些属于项目级性能基线，哪些应下沉到具体模块或专项性能文档。

16. **后台邮件模板配置能力**
- **管理能力补齐**:
    - 在后台管理中增加邮件模板配置入口，支持模板选择、变量说明、启停、预览或等效最低可用能力。
- **规范联动**:
    - 与邮件模块设计、邮件服务规范及后台设置能力保持一致，避免再次形成两套口径。

17. **scripts 目录 README 与入口索引**
- **脚本说明文档**:
    - 为 `scripts/` 目录补 README，说明脚本分类、使用方式、入口命令、输入输出、副作用范围与调用场景。
- **引用关系梳理**:
    - 说明脚本分别被哪些 `package.json` 命令、CI、规范文档或治理流程使用。

### 其他 backlog

1. **认证会话获取频率治理与 `/api/auth/get-session` 调用收敛**
- **调用链审计**:
    - 梳理公开端、管理端、插件、中间件、composable 与布局层对 `/api/auth/get-session` 的调用来源，区分 SSR 首屏、客户端 hydration、路由切换、聚焦恢复与组件重复挂载等不同触发路径。
- **缓存与去重策略**:
    - 评估会话读取在 `authClient`、`useAsyncData`、全局状态与请求层中的短时缓存、请求合并、失效时机与跨页面复用策略，避免每次渲染或导航都重复命中会话接口。
- **基线与回归验证**:
    - 为登录态初始化、登出同步、会话过期、多标签页切换与页面刷新建立调用次数基线和回归测试，确保降频后不破坏权限判断与用户体验。

2. **RSS 资源挂载与外部动态聚合展示**
- **RSS / RSSHub 接入层**:
    - 为 RSS 与 RSSHub 源建立统一拉取、清洗、缓存与失败降级层，支持将其他平台的动态或文章映射为站内可消费的数据结构，而不是在页面中直接拼接外部返回结果。
- **页面挂载模型**:
    - 支持将外部内容以卡片流、时间线或侧栏模块挂载到首页、作者页或独立聚合页，并明确排序、去重、来源标识、语言过滤与跳转策略。
- **稳定性与治理边界**:
    - 明确抓取频率、缓存时效、可选关闭开关、来源归属说明与解析失败回退方案，避免外部 RSS 源抖动或 RSSHub 不可用时拖垮主站页面稳定性。

3. **`caomei-auth` 第三方登录支持评估与接入预研**
- **协议与能力基线**:
    - 评估 `caomei-auth` 是否提供可被 `better-auth` / `genericOAuth` 稳定消费的 OAuth 2.1 / OIDC 能力，至少包括授权地址、令牌地址、用户信息地址或等效 discovery 信息，以及 `state`、PKCE、`nonce`、错误码和 scope 约束。
    - 明确登录、刷新、登出、令牌撤销、JWK / 签名校验与多环境回调地址的支持边界，避免接入后只能“能登录一次”，却无法稳定维护会话生命周期。
- **账户映射与资料契约**:
    - 明确 `caomei-auth` 返回的稳定用户标识、`email` / `email_verified`、昵称、头像、locale 等字段契约，确保与现有 `better-auth` 用户模型、账号绑定与多语言界面兼容。
    - 评估同邮箱合并、已存在账号绑定、第三方账号解绑与异常回退策略，避免引入重复账号或破坏现有认证链路。
- **配置、安全与反向需求输出**:
    - 梳理接入所需的 Client ID / Secret、回调 URL、权限范围、品牌文案、图标、合规提示与 ENV 锁定策略，确认哪些配置必须继续维持部署期环境变量管理。
    - 基于接入过程反推 `caomei-auth` 需要补齐的能力，例如 discovery 文档、标准 claims、刷新令牌、撤销接口、JWK 暴露、租户隔离或审计字段，再决定是否正式排期。

4. **多语言社交与赞助平台扩展**
- **平台扩容**:
    - 在现有社交 / 赞助系统上继续补齐默认平台，优先覆盖掘金、小红书、抖音等高频中文平台，和 StackOverflow、Instagram、LinkedIn、Twitch 等英文平台，并同步提供图标、颜色与展示文案约定。
- **语言一致性**:
    - 平台名称、标签与编辑选项需覆盖当前项目已支持的语言集合，避免仅有中文 / 英文而在 `zh-TW`、`ko-KR` 下退回硬编码平台名。
- **兼容边界**:
    - 扩容不得破坏作者级 / 全局级回退、locale 过滤、自定义平台与二维码 / URL 双模式展示链路。

5. **存量资源链接重写与迁移工具**
- **重写与预览能力**:
    - 支持对存量资源链接执行批量重写、域名过滤、dry-run 预览与差异报告导出，降低对象存储后端切换与 CDN 域名迁移成本。
- **配置与安全边界**:
    - 对齐当前存储配置、路径前缀、公共 Base URL 与权限模型，避免重写任务绕开既有上传与访问控制链路。
- **验证与回退**:
    - 补齐最小自动化验证，至少覆盖仅预览、部分命中、全量重写与失败回退 4 类场景。

6. **桌面端应用 (Desktop Application)**
- **Tauri 跨平台应用**:
    - 实现桌面客户端骨架，支持单站点/多站点管理。
    - 支持离线 Markdown 写作与间断性云端同步功能。
    - 提供原生菜单和系统集成，提升桌面体验。

7. **极客技术增强 (Geek Tech Extras)**
- **可执行代码块支持**:
    - 实现 Markdown 代码块在特定环境下的运行与结果输出。
    - 支持常见编程语言的代码执行（如 JavaScript、Python、Shell）。

8. **主题生态系统 (Theme Ecosystem)**
- **主题社区与发布平台**:
    - 允许创作者发布、分享并由他人安装自定义主题。
    - 建立安全审核机制防范 XSS 攻击。
- **画廊与市场**:
    - 支持保存多套自定义配置，建立主题预览、发布与管理中心。
    - 提供主题评分、评论和下载统计。
- **安全性与性能**:
    - 实现针对发布主题的自动化 XSS 扫描。
    - 使用 CSS Layer 分层策略提升主题性能。

9. **商业化与会员体系 (Monetization & Membership)**
- **支付体系集成**:
    - 集成 Stripe/支付宝支付接口。
    - 支持打赏功能或 Pro 会员准入策略。

10. **播客与多媒体扩展 (Podcast & Multimedia)**
- **全站沉浸式播放**:
    - 全站悬浮播放器，支持跨页面断点续播与内容同步。

11. **AI 视频生成与增强 (AI Video Generation & Enhancement)**
- **多模态内容产出**:
    - 探索集成视频生成模型（如 Seedance 2.0、Sora 2.0 等），支持基于文章内容或脚本生成动态视频素材。
    - 实现“文章转视频”工作流，为技术博文自动生成短视频概览或演示。

12. **文章详情相关文章推荐与内容发现增强 (Related Content Discovery Enhancements)**
- **优先级重评估**:
    - 作为第十三阶段中的可选增强项，复审后 ROI 约为 1.25，当前不满足进入阶段核心任务的准入基线。
    - 仅在配置国际化、迁移守卫与验证基线完成后，再评估是否上收至后续阶段。
- **推荐策略**:
    - 在文章详情页按分类、标签或 translation cluster 相似度提供相关文章推荐，并避免同一翻译簇重复占位。
- **展示与验证**:
    - 推荐模块应以不打断正文阅读的轻量形态落地，并补齐多语言过滤、去重与推荐规则回归测试。

13. **安装向导与后台设置多语言字段收敛**
- **统一配置模型**:
    - 安装向导中的站点标题、描述、品牌文案等初始化字段，应与后台设置页复用同一套 locale-aware settings 模型、回退解析器与元信息结构，避免再维护一套只在 installation 生效的配置事实源。
- **交互边界**:
    - installation 页面默认只展示当前用户所选语言对应的输入值，初次填写时直接落在当前 locale；不在安装页暴露完整多语言编辑器，多语言扩展统一延后到后台设置页处理，保持首配流程简洁。
- **回归要求**:
    - 补齐安装流程、后台设置页与公开设置消费链路的定向回归，确认默认语言值、多语言追加编辑与 legacyValue 兼容不会互相覆盖，也不会在初始化后出现字段缺失。

14. **README 多语言镜像存放位置重评估**
- **目录方案评估**:
    - 评估是否将 `README.<locale>.md` 迁移到 `docs/` 下的专门目录（如 `docs/readme/` 或 `docs/i18n/<locale>/readme/`），但必须先明确 GitHub 仓库入口、外部引用与文档站导航是否仍需保留根目录镜像或兼容跳板。
- **规范与链接同步**:
    - 若目录调整成立，需要同步修正文档规范、VitePress 导航、README 互跳链接、翻译治理说明与外部链接映射，避免出现“仓库入口一套、文档站一套”的双重事实源。
- **迁移门槛**:
    - 在未证明不会削弱 GitHub 首屏可读性、不会破坏现有多语言 README 入口之前，该事项维持 backlog 评估，不直接纳入当前阶段执行。

15. **AI 初始化 / 问答助手评估与工程复用治理**
	- 验收: 评估是否提供面向新部署者的 AI 初始化助手或配置问答助手，先收敛可回答范围、可写配置范围、ENV 锁定字段、越权保护与失败回退策略，再决定是否立项实现。
	- 验收: 若进入实现阶段，应优先复用现有安装向导、Settings 元信息、`qa-assistant` 与文档知识源，而不是额外维护第二套配置事实源。

## 相关文档

- [项目计划](./roadmap.md)
- [待办事项](./todo.md)
- [待办归档](./todo-archive.md)
- [项目规划规范](../standards/planning.md)
